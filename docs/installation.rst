============
Installation
============

At the command line::

    $ easy_install django-self-aware-model

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-self-aware-model
    $ pip install django-self-aware-model
